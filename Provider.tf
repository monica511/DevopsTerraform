provider "aws" {
  profile = "Monica Patil"           # this the profile which we created using aws configure cli command
  region  = "ap-south-1"        # this will make default region as ap-south-1 which is in mumbai
}
